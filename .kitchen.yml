---
driver:
  name: docker

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  - name: ubuntu-14.04
  - name: centos-6.4

suites:
  - name: default
    run_list:
      - recipe[minitest-handler]
      - recipe[build-essential]
      - recipe[vault-cli::default]
    attributes: {}
