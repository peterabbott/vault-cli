lanuage: ruby
bundler_args: "--without kitchen_docker --without development --jobs 5"
rvm:
- 2.1.0
- 2.0.0
- ruby-head
cache: bundler
script:
- bundle exec thor :ci
matrix:
  allow_failures:
  - rvm: ruby-head
branches:
  except:
  - "/^[0-9]/"
